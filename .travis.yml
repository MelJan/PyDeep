language: python
python:
  - "2.7"
cache: pip
install:
  - pip install coverage
  - pip install coveralls
  - pip install matplotlib
  - pip install numpy
  - pip install scipy
  - pip install .
script:
  - travis_wait 60 coverage run -m unittest discover
after_success:
  - coveralls
